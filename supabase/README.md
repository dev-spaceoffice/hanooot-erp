# Supabase scaffold

This folder is reserved for forward-only SQL migrations and generated database types in later backend slices.

- Put migrations in `supabase/migrations/`.
- Enable RLS for application tables during schema implementation.
- Keep service-role usage server-only; this foundation branch only includes anon-key client helpers.
